"use client";

import TemplateWizard from "@/components/tools/generador-anuncios/TemplateWizard";

// El flujo de PLANTILLA. El clásico (subir tu propia referencia) vive en /wizard y no se toca.
export default function GeneradorAnunciosPlantillas() {
  return <TemplateWizard />;
}
